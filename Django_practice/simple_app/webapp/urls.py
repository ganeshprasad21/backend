from django.urls import path
from webapp import views
# from webapp_another import views

urlpatterns = [
    # path('admin/', admin.site.urls),
    path('x',views.ganesh_prasad_r),
    # path('y',views.newGaneshPrasadR)
]
